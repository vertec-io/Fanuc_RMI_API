[package]
name = "web_common"
version = "0.1.0"
edition = "2021"
description = "Shared API types for FANUC RMI web application"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
fanuc_rmi = { path = "../fanuc_rmi", default-features = false, features = ["DTO"] }

